# M2.9 Next Work

Recommended next steps after v0.2-alpha:

1. Improve the hobbyist local daemon experience and status output.
2. Add a receive-only Dire Wolf TCP KISS walkthrough with screenshots or copied terminal output.
3. Complete Mercury OFDM interface discovery from documentation and black-box observation.
4. Add a no-TX Mercury status probe only after its interface is documented.
5. Add receive-only external modem capture import fixtures.
6. Keep CONNECT and TX blocked until operator, port, dispatch, PTT, and channel policy are designed.
7. Keep FX.25 separate from Mercury and AX.25 work.
