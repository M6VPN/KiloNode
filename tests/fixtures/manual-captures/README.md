# Manual Capture Test Fixtures

These fixtures exercise the manual receive-only capture workspace without real
radio hardware. They are synthetic `.capture` files used as import sources.

They are not LinBPQ captures, are not copied from GPL code, and do not require
transmit capability.
