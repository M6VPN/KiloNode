# AX.25 Timer Replay Fixtures

These fixtures are synthetic KiloNode-native timer replay scripts. They use
injected monotonic time and do not require RF hardware, KISS transports,
threads, sleeps, daemon scheduling, or transmit paths.

The scripts validate diagnostic AX.25 connected-mode timer behaviour only.
FX.25 is out of scope for these fixtures.
